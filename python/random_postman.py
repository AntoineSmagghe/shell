#!/bin/python3
import random

team = [
    ('Emilie', 0.1),
    ('Gregory', 0.1),
    ('Ricardo', 0.2),
    ('Antoine', 0.1),
    ('Bertrand', 0.1)
]

teamTMAVimeet = [
    ('Emilie', 0.1),
    ('Gregory', 0.1),
    ('Ricardo', 0.2)
]

if postman in teamTMAVimeet

def calculateWithWeight(arr):
    maxArray = [(i, sum(p for j,p in arr if j < i)) for i,_ in arr]
    return max(i for r in [random.random()] for i,c in maxArray if c <= r)

postman = calculateWithWeight(team)

print("\nFacteur :", calculateWithWeight(team))
print()
print("TMA Vimeet :", calculateWithWeight(teamTMAVimeet), "\n")
